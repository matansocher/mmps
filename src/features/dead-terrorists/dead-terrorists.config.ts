import { CHANNELS } from '@services/telegram-client';

export const TELEGRAM_CHANNELS_TO_LISTEN = [
  CHANNELS.ISRAEL_TECH_FORUM.id,
  CHANNELS.TROLL_FOOTBALL.id,
  CHANNELS.UNCENSORED_ISRAEL_NEWS.id,
  CHANNELS.OVADIA_TUBUL.id,
  CHANNELS.GADGETY.id,
  CHANNELS.MACCABI_HAIFA_FANS.id,
  CHANNELS.A_MOMENT_BEFORE.id,
  CHANNELS.GEEKTIME.id,
  CHANNELS.TRANSFERMARKT.id,
  CHANNELS.TOOTIE.id,
  CHANNELS.ERAN_SWISSA.id,
  CHANNELS.CALCALIST.id,
  CHANNELS.TRANSFER_NEWS_FOOTBALL.id,
  CHANNELS.SKY_SPORTS_FOOTBALL.id,
  CHANNELS.NEWS_FROM_THE_FIELD.id,
  CHANNELS.FATAL_ERROR.id,
  CHANNELS.ABU_ZALAH.id,
  CHANNELS.DANIEL_AMRAM.id,
  CHANNELS.MACCABI_HAIFA_TELEGRAM.id,
  CHANNELS.ENGINEERING_TRICKS.id,
  CHANNELS.SHLOMO_WEATHER.id,
  CHANNELS.KVUZA.id,
  CHANNELS.ESPN_FOOTBALL.id,
  CHANNELS.DARK_SPACE.id,
  CHANNELS.AI_ART.id,
  CHANNELS.ALMOG_BOKER_UPDATES.id,
  CHANNELS.CENTRE_GOALS.id,
];
