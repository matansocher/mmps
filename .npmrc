# Always include optional dependencies in installs and lockfile entries.
# This avoids the macOS-vs-Linux lockfile drift bug (npm/cli#4828) where
# packages like sharp/canvas/emnapi end up missing platform-specific
# optionalDependencies in the lockfile, breaking `npm ci` on Linux CI.
include=optional
